# 完整首轮任务

离线仿真模块要对稀疏SPD系统求解，并避免递推残差误报收敛。请从零实现“稀疏线性系统预条件迭代求解库”。

1. 实现COO到CSR及SPD求解，1<=n<=256、输入三元组<=10000；用math.fsum合并重复值并去除精确0，排序规范化。拒绝非法索引/非有限值；合并后A[i,j]与A[j,i]须精确相等，缺项视为0，不偷偷对称化或稠密化。
2. 正定性是调用者前提，不要求完整预检。实现PCG，预条件Jacobi或IC(0)；IC只保留原下三角模式，不加填充、扰动或隐式切换，非正主元/非正曲率/非有限中间值返回breakdown。
3. 支持给定初值，atol/rtol有限非负且至少一个为正，0<=max_iter<=500。先计算真实残差，已满足||b-Ax||2<=atol+rtol*||b||2时返回0迭代converged；否则才构建预条件。
4. 每10迭代及候选收敛/退出时重算真实残差；候选未达标则按真实残差重启方向，预算耗尽返回max_iter。返回迭代数、带迭代编号的真实残差记录和实际matvec次数，不把递推残差冒充真实残差。
5. 用n<=8独立高斯消元参考、一个已知解的稀疏案例及失败案例验证；不保证IC不失败或一定加速。无numpy/scipy，演示两种预条件及状态；不做大型基准或通用矩阵平台。

验收重点：重复COO、非对称拒绝、缩放对角、IC主元失败、零右端非零初值、0迭代初值解、max_iter、真实残差。

工程约束：全新0-1核心库，仅用Python 3.14.7标准库，在Windows原生离线运行，无第三方依赖、外部服务、Docker或前后端。交付实现、聚焦上述边界的测试及简短README；演示命令`python demo.py`用小案例在约8秒内打印正常结果和一种真实失败，测试命令`python -m unittest discover -s tests -v`。固定种子随机验证只用少量小样本，避免长压测和额外平台功能，不限制正常纠错所需工具调用。
一次首轮完成，不要求用户追加“继续”。只操作当前仓库，不读取用户配置、密钥、环境变量清单或其他工作区；不添加Actions/CI/部署/Dependabot、不远程推送，不硬编码演示或虚报验证。
