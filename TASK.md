# 完整首轮任务

请从零实现“稀疏线性系统预条件迭代求解库”，用于数值计算 / 稀疏矩阵中的本地可复用模块。

1. 从零实现实数稀疏SPD线性系统求解库。输入COO三元组，合并重复项、去除精确零项并转为规范CSR；拒绝非法索引和非有限数。维数1..2000，禁止numpy/scipy以及构造n×n稠密业务矩阵。
2. 验证数值对称性，给定atol/rtol；正定性是调用者前置条件，但发现非正曲率或预条件异常必须返回breakdown，不能声称对所有非SPD输入都能预检。支持零初值和给定初值。
3. 实现预条件共轭梯度，预条件可选Jacobi或IC(0)；IC(0)仅保留原下三角稀疏模式，分解出现非正主元明确失败，不偷偷加对角扰动或切换算法。
4. 收敛以真实残差||b-Ax||2<=atol+rtol*||b||2为准；定期重算真实残差，递推残差声称收敛时必须复核，必要时重启方向。返回converged/max_iter/breakdown、迭代数、真实残差历史和matvec计数。
5. 用小矩阵的独立稠密高斯消元参考验证；大案例用已知x生成b，并展示两种预条件结果。允许最大500迭代，不要求特定加速倍数；记录0右端、非SPD和迭代上限情况，不包装通用科学计算平台。

验收重点：重复COO、孤立对角、病态对角缩放、IC主元失败、零右端非零初值、最大迭代、非对称拒绝、真实残差。

工程范围：这是全新核心库的0-1实现，初始仓库仅有运行骨架，没有业务实现。仅使用Python 3.14.7标准库，在Windows原生环境离线运行；无需安装第三方依赖。不接数据库、中间件、云API、外部程序，不使用Docker，不制作前后端或通用CLI。
交付：实现可复用库、聚焦上述语义的自动化测试和README；python demo.py须在约8秒内运行确定性小案例，打印关键正常结果和至少一种真实边界失败，便于录制十几秒视频。测试命令为python -m unittest discover -s tests -v。先阅读本仓库README和TASK.md，简短计划后集中实现，再运行针对性测试与演示；避免反复通读仓库、无关探索和无必要的长篇文档，发现实际问题时正常修复验证，不设硬性工具调用截断。
只能操作当前仓库内文件；不读取用户配置、密钥、环境变量清单或其他工作区。不要新增GitHub Actions、CI、部署、Dependabot或远程推送。一次首轮完成，不要求用户追加“继续”；合理实现取舍写入README。不要为通过测试删除需求、硬编码示例输出或宣称未实际执行的验证通过。
