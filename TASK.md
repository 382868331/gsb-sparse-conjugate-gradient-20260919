# 完整首轮任务

请从零实现稀疏线性方程组求解库，使用预条件共轭梯度法。除了得到解，还需要让调用方知道何时收敛、何时发生数值失败，残差和预条件器按以下规则处理。

实现COO到CSR及SPD求解，1<=n<=256、输入三元组<=10000；用math.fsum合并重复值并去除精确0，排序规范化。拒绝非法索引/非有限值；合并后A[i,j]与A[j,i]须精确相等，缺项视为0，不偷偷对称化或稠密化。

正定性是调用者前提，不要求完整预检。实现PCG，预条件Jacobi或IC(0)；IC只保留原下三角模式，不加填充、扰动或隐式切换，非正主元/非正曲率/非有限中间值返回breakdown。

支持给定初值，atol/rtol有限非负且至少一个为正，0<=max_iter<=500。先计算真实残差，已满足||b-Ax||2<=atol+rtol*||b||2时返回0迭代converged；否则才构建预条件。

每10迭代及候选收敛/退出时重算真实残差；候选未达标则按真实残差重启方向，预算耗尽返回max_iter。返回迭代数、带迭代编号的真实残差记录和实际matvec次数，不把递推残差冒充真实残差。

用n<=8独立高斯消元参考、一个已知解的稀疏案例及失败案例验证；不保证IC不失败或一定加速。无numpy/scipy，演示两种预条件及状态；不做大型基准或通用矩阵平台。

测试里请覆盖这些情况：重复COO、非对称拒绝、缩放对角、IC主元失败、零右端非零初值、0迭代初值解、max_iter、真实残差。

使用 Python 3.14.7 标准库，代码要能在 Windows 原生环境离线运行。范围就是这个库，不需要第三方依赖、外部服务、Docker 或前后端。请一并写好测试和简短的 README，说明接口及运行方法。

用 `python demo.py` 运行演示，在约 8 秒内展示一个正常结果和一个实际触发的失败；用 `python -m unittest discover -s tests -v` 运行测试。随机验证采用固定种子和少量小样本即可，不做长时间压力测试，也不扩展额外的平台功能。

请在本轮完成实现和验证，不要等我回复“继续”。正常排错需要的工具调用不设次数限制。操作范围限于当前仓库，不读取用户配置、密钥、环境变量清单或其他工作区。不要添加 Actions、CI、部署或 Dependabot，也不要向远端推送。演示结果应由代码实际计算；最后如实说明验证结果，未运行或未通过的检查也请写明。
